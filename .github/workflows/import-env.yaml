name: Import env artifact

on:
  workflow_call:

jobs:
  import-env:
    runs-on: self-hosted
    steps:
      - name: Download env artifact
        uses: actions/download-artifact@v4
        with:
          name: env

      - name: Import env
        run: |
          while read env; do
            echo $env >> $GITHUB_ENV
          done <<< $( cat .env)